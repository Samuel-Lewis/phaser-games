export const nineSliceDimensions = {
  topHeight: 12,
  bottomHeight: 12,
  leftWidth: 8,
  rightWidth: 8,
  dropBuffer: 2,
};

export type ButtonColour = 'blue' | 'green' | 'red' | 'yellow' | 'grey';
