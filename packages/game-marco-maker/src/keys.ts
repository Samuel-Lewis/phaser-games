export enum SceneKeys {
  Boot = 'boot',
  StartScreen = 'start-screen',
  Player = 'player',
  Editor = 'editor',
  EditorArea = 'editor-area',
}

export enum RenderLayer {
  Background = 0,
  Game = 100,
  Character = 200,
  UI = 300,
}
