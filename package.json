{
  "name": "phaser-games",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write **/*.{ts,tsx,md}"
  },
  "devDependencies": {
    "@samuel-lewis/eslint-config": "workspace:*",
    "@samuel-lewis/typescript-config": "workspace:*",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "lint-staged": {
    "*.{ts,tsx,md}": "prettier --write **/*.{ts,tsx,md}"
  }
}
